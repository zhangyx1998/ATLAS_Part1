GRANT SELECT,INSERT,UPDATE,DELETE ON ATLAS_main.* TO 'ArduinoIO'@'localhost' IDENTIFIED BY '***' WITH GRANT OPTION;

mariadb>
CREATE TABLE ARDUINO_IO (
TS DOUBLE NOT NULL,
ECC TINYINT default 0,
Env_Temp double default 0,
Env_Humidity double default 0,
Last_Update TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
PRIMARY KEY (TS)
);
